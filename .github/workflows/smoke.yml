name: Disabled — Smoke test

on:
  workflow_dispatch: {}

jobs:
  noop:
    name: Disabled smoke job
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "Workflow smoke dinonaktifkan. Untuk mengaktifkan lagi, restore versi sebelumnya atau edit workflow."
